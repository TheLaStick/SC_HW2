package LaStick;

public class Space extends Origin {
    /**
     * Создает мировую систему координат
     * @param position Координаты
     */
    public Space(Coord2D position) {
        super(position);
    }
}
